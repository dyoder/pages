view :image, :quick_add
ul do
  @images.sort_by(&:title).each do |image|
    li do
      a :href => "/admin/image/#{image.key}/" do
          img :src => "/images/#{image.title}?size=thumb", 
          :title => image.title, :alt => image.title
      end
    end
  end
end