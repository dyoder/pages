view :calendar, :quick_add
ul do
  @calendars.select { |cal| cal.format != 'mab' }.sort_by(&:title).each do |calendar|
    li do
      a calendar.title, :href => "/admin/calendar/#{calendar.name}/"
    end
  end
end