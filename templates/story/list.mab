view :story, :quick_add
ul do
  @stories.select { |story| story.format != 'mab' }.sort_by(&:title).each do |story|
    li do
      a story.title, :href => "/admin/story/#{story.name}/"
    end
  end
end