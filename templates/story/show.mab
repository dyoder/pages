layout :show, :title => @story.title do
  div.main.story :id => @story.name do
    view( :story, :content, :story => @story )
  end  
#  if @story.comments
#  	h3 @story.comment_number
#  	view :comment, :list, :comments => @story.comments
#  end
#  view :comment, :add, :entry => @story
end